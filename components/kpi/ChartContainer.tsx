import { Card } from '@/components/ui/Card';

// pfe-next/components/kpi/ChartContainer.tsx
type ChartContainerProps = {
  title: string;
  subtitle: string;
};

export function ChartContainer({ title, subtitle }: ChartContainerProps) {
  return null;
}
